datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_INTERACT_URI")
}

model likes {
  id        Int      @id @default(autoincrement())
  userid      String
  trackid     String
  quantity Int @default(0)
}
